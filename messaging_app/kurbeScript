#!/bin/bash
# Starts Kubernetes cluster, verifies it, lists pods

if ! command -v minikube >/dev/null 2>&1; then
    echo "Minikube is not installed."
    exit 1
fi

minikube start
kubectl cluster-info
kubectl get pods --all-namespaces

