apiVersion: apps/v1
kind: Deployment
metadata:
  name: django-blue
spec:
  replicas: 2
  selector:
    matchLabels:
      app: django-blue
  template:
    metadata:
      labels:
        app: django-blue
    spec:
      containers:
      - name: django-blue
        image: your-dockerhub-username/django-app:blue
        ports:
        - containerPort: 8000
