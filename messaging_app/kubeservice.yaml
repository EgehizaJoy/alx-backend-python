apiVersion: v1
kind: Service
metadata:
  name: django-service
spec:
  selector:
    app: django
    version: blue   # Start with blue, switch to green later
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
